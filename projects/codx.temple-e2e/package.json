{
  "name": "codx.temple-e2e",
  "private": true,
  "version": "0.1.0",
  "packageManager": "pnpm@11.18.0",
  "type": "module",
  "scripts": {
    "test": "node -e \"if (!process.env.CI) { require('child_process').spawnSync('npx', ['playwright', 'test'], {stdio:'inherit', shell:true}) } else { console.log('E2E tests skipped in CI (requires API + UI servers)') }\"",
    "test:ui": "playwright test --ui",
    "test:debug": "playwright test --debug",
    "report": "playwright show-report"
  },
  "devDependencies": {
    "@playwright/test": "^1.62.1"
  }
}
